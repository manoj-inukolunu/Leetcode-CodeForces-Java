package com;

public class Leetcode {


 /* @Test
  public void diffArray() {
    Map<Integer, Integer> map = new HashMap<>();
    int[][] intervals = new int[][] {{0, 8}, {3, 19}, {4, 6}};
    for (int[] interval : intervals) {
      map.put(interval[0], map.getOrDefault(interval[0], 0) + 1);
      map.put(interval[1] + 1, map.getOrDefault(interval[1], 0) - 1);
    }
    Map<Integer, Integer> qMap = new HashMap<>();
    List<Entry<Integer, Integer>> entries = new ArrayList<>(map.entrySet());
    entries.sort(Comparator.comparingInt(Entry::getKey));
    int[] queries = new int[] {1, 3, 5, 7, 9};
    List<Integer> list = new ArrayList<>();
    list.add(entries.get())
    for (int query : queries) {
      System.out.println(arr[query]);
    }
  }*/

}
;